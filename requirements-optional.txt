# Optional: adds the UMAP baseline to 15_e10_embeddings.py. Without it the
# script records "umap_status: 未実施" and skips that one baseline; nothing else
# changes.
#
# Kept out of requirements.txt because umap-learn pulls in numba, which lags new
# Python releases — on Python 3.14 the numba build fails outright. It installs
# and runs on the supported Python 3.11.
-r requirements.txt
umap-learn==0.5.12
