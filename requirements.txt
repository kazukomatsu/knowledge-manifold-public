# Core dependencies. Python >= 3.11 is required — every pin below declares
# Requires-Python >= 3.11, and Python 3.9 additionally changes the results:
# the anchor-assignment tie-break shifts and E1 drifts by ~0.02
# (spearman 0.676 instead of 0.687). See KNOWN_ISSUES.md #2.
#
# Verified working set (Python 3.11.15, macOS arm64, 2026-08-06): all 15
# validation gates pass and the published E1/E2 values reproduce to 3 decimals.
numpy>=2.4.6
scipy>=1.17.1
scikit-learn>=1.9.0
matplotlib>=3.11.1
